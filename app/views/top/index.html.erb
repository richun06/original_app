<div class="bg">
    <h1 class="logo">Carely</h1>
    <h3 class="logo">離れて暮らすご家族健康状態がわかる</h3>
    <p class="logo">ご家族とスムーズに情報共有することができる</p>
    <h3 class="logo">介護に集中できる</h3>
    <p class="logo">現場のペーパーレス化を促進し、より良いケアを提供する</p>
    <h3 class="logo">人為的なミスを減らす</h3>
    <% if user_signed_in? %>
      <%= link_to "マイページへ", user_path(current_user.id) %>
    <% else %>
      <%#= link_to "新規登録", new_user_registration_path, class: 'post' %>
      <%#= link_to "ログイン", new_user_session_path, class: 'post' %>
      <%= link_to 'ゲストログイン(施設側)', users_guest_sign_in_path, method: :post %>
      <%= link_to '管理者ゲストログイン', users_admin_guest_sign_in_path, method: :post %>
    <% end %>
</div>
